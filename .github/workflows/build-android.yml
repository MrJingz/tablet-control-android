name: Build Android APK

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Install GraalVM JDK 17
      uses: graalvm/setup-graalvm@v1
      with:
        version: '22.3.3'
        java-version: '17'
        components: 'native-image'
        github-token: ${{ secrets.GITHUB_TOKEN }}
        set-java-home: 'true'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Cache Maven dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
        
    - name: Verify Environment
      run: |
        echo "Java version:"
        java -version
        echo "JAVA_HOME: $JAVA_HOME"
        echo "GRAALVM_HOME: $GRAALVM_HOME"
        echo "PATH: $PATH"

    - name: Test Basic Compilation
      run: |
        cd gluon-crossplatform
        mvn clean compile

    - name: Build JAR Package
      run: |
        cd gluon-crossplatform
        mvn package -DskipTests

    - name: List Build Output
      run: |
        cd gluon-crossplatform
        find target -name "*.jar" -ls || echo "No JAR files found"

    - name: Build Android APK (if JAR succeeds)
      run: |
        cd gluon-crossplatform
        mvn gluonfx:build -Pandroid
        
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: tablet-control-build
        path: |
          gluon-crossplatform/target/*.jar
          gluon-crossplatform/target/gluonfx/**/*.apk
        
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.run_number }}
        name: Android APK v${{ github.run_number }}
        body: Automatically generated Android APK build
        files: gluon-crossplatform/target/gluonfx/aarch64-android/*.apk
